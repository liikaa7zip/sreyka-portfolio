name: Deploy Static Website

on:
  push:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show files
        run: ls -la

      - name: CI/CD Working
        run: echo "CI/CD is running successfully 🚀"
